# Chunk JSONL is no longer exported to this directory.
#
# The canonical copy is the immutable `chunk_index` Artifact referenced by
# `papers.chunk_index_artifact_id` and stored below `APP_STORAGE_DIR`.
# Retrieval and indexing resolve that Artifact through the database so
# workspace ownership and storage paths stay consistent.
#
#
# This README is retained only to explain the retired path. New deployments
# do not need `backend/data/chunks/`.
#
