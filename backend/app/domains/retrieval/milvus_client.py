"""Retrieval Milvus client - Phase 2 placeholder."""

from __future__ import annotations

# Phase 2 will add:
# - Milvus collection setup (paper_chunks)
# - embed_and_insert()
# - semantic_search(workspace_id, query, k)
# - similar_works(paper_id, k)
# - counter_evidence(claim_id, k)
